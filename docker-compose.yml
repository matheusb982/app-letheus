services:
  mongodb:
    image: mongo:7
    container_name: letheus-mongodb
    ports:
      - "27018:27017"
    volumes:
      - letheus-mongo-data:/data/db
    restart: unless-stopped

volumes:
  letheus-mongo-data:
